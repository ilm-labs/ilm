sudo: false

language: ruby

rvm:
  - 2.6.3

services:
  - docker

before_install:
  - docker build -t console .
  - docker run -v ~/ilm:/ilm:ro console /bin/sh -c "cd /root/sinatra; bundle exec rake test"
  - docker ps -a
  - gem install bundler

script:
  - bundle exec rake test
